# Decision Tracker

| Date/Time | Decision Made | Reasoning | Outcome |
|-----------|---------------|-----------|---------|
| 2026-03-13 | Created decision tracker for Madi's project | Establishing a structured log to record key decisions, their reasoning, and outcomes throughout the project lifecycle | Decision tracker initialized and ready for use |
| 2026-03-13 | Built Calendar Invite web app with React + Express | Small internal team needs to create Outlook calendar invites from documents or natural language. React/Vite chosen for clean UI; Express (CommonJS) chosen for broad npm compatibility with pdf-parse and mammoth. | Full app scaffolded with AI extraction, MS Graph API integration, and editable preview UI |
| 2026-03-13 | Used Vite dev proxy for auth/API calls | Cross-origin session cookies require HTTPS + sameSite=none in production but are painful on localhost. Proxying /api and /auth through Vite makes both origins appear same-site to the browser, so sameSite=lax + HTTP works in development. | Zero CORS configuration needed during development; auth flow works cleanly |
| 2026-03-13 | Used delegated OAuth (auth code flow) instead of app-only credentials | Delegated flow means calendar events are created as the signed-in user (correct organizer identity, proper email sender). App-only credentials would require admin-consented Application permissions and would create events from a service account, losing the organizer context. | Events appear in the user's own Outlook calendar with correct organizer name |
| 2026-03-13 | Used Claude claude-opus-4-6 for event extraction with a structured system prompt | claude-opus-4-6 reliably returns valid JSON when given clear extraction instructions. System prompt specifies exact field names and formats; response is parsed with a code-fence-stripping fallback for robustness. | Consistent, structured event extraction from both free-text prompts and documents |
